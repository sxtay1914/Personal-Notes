### Codebase RT
1. In activity-guard.ts: 
* the reason why context and workspace id is needed for cache key is because there are 2 different modes (stdio and http)
* having a ttl flag in FlaggedCache entry is a bit insensible
* 4 steps: 
    * confidence gating (blocks call if confidence is too low and prevent resubmission of entries with higher confidence level to prevent ai from gamify the system
    * change_state requires human approval: too sensitive
    * change_param requires human approval: same reason as above
2. In index.ts:
* asyncLocalStorage is actually used to solve concurrency issue by giving async requests its own storage by going though its own await points. (**Use this for async operations) 
