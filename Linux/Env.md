## Commands
* printenv
* set
* alias
* export

## What's in shell?
* shell var, env var, shell func and aliases

## What is actually PATH?
* A list of dir where the execution files are stored, so for example when we type ls, only the dir in the paths will be searched instead of the whole comp

## Important files to modify PATH
* ~/.bash_profile (a specific user)
* ~/.bashrc (everything else)


